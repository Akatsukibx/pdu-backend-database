//backend
DB_HOST=localhost        # ที่อยู่เครื่อง Database
DB_PORT=5432             # พอร์ต PostgreSQL
DB_NAME=pdu_db           # ชื่อฐานข้อมูล
DB_USER=dusitmuangmee    # user สำหรับเชื่อมต่อ DB
DB_PASSWORD=root         # รหัสผ่าน DB

SNMP_VERSION=2c          # เวอร์ชัน SNMP ที่ใช้ดึงข้อมูล PDU
POLL_INTERVAL=60000      # ดึงข้อมูลจาก PDU ทุก 1 นาที
POLL_LOG_TABLE=off       # เปิด/ปิด log ตารางตอน poll

ALLOWED_USERNAME=admin   # username ที่อนุญาตให้ login
JWT_EXPIRES_IN=10m       # อายุ token หลัง login
JWT_SECRET=ใส่ยาวๆสุ่มๆ # key สำหรับเข้ารหัส token

SESSION_MINUTES=10       # อายุ session สูงสุด
SESSION_IDLE_MINUTES=5   # ไม่ใช้งานเกินกี่นาทีให้หลุด
SESSION_CLEANUP_INTERVAL_MS=60000 # ล้าง session ทุก 1 นาที

USAGE_CURRENT_THRESHOLD=0.1
# current > 0.1A = เริ่มนับ uptime
# current <= 0.1A = หยุดและสรุปเวลาใช้งาน


//frontend
VITE_API_BASE=http://localhost:8000     # URL ของ Backend API ที่ Frontend เรียกใช้งาน
VITE_APP_NAME=PDU Monitoring System     # ชื่อระบบ แสดงบนหน้าเว็บ
VITE_COMPANY=LannaCom                   # ชื่อหน่วยงาน/บริษัท (ใช้แสดงผล)
VITE_MAX_SESSIONS=2                     # จำกัดจำนวน session ที่ login ได้พร้อมกัน
VITE_SHOW_DEBUG=false                   # เปิด/ปิดโหมด debug บนหน้าเว็บ
